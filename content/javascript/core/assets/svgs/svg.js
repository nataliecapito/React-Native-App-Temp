// general: most or all svgs used can be found here.

// important: if you insert an .svg and it does not have a 'fill' loacted in:
// '<path fill="some color"',
// please manually add the 'fill' attribute (fill="white"), else the .svg will be
// the color black FOREVER.

// important: correct format for adding new .svgs is
// (WHATEVER_SVG_NAME: '.svg path',) the 'comma', at the end, is very important or
// this whole file will fail!

// THANKS!

export default {
  // GENERAL ICONS
  APP_ICON_SVG: '',
}
